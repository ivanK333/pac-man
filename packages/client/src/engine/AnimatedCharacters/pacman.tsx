export const drawPacman2 = (ctx: CanvasRenderingContext2D) => {
  ctx.save();
  // Issue drawing commands, assuming some 0,0 center and an unrotated bug
  // Use the current time, or some frame counter, to change how things are drawn

  ctx.restore();
};
