{
  "name": "client-server-template-with-vite",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && node init.js && lerna clean && yarn && lerna bootstrap",
    "build": "lerna run build",
    "dev:client": "lerna run dev  --scope=client",
    "dev:server": "lerna run dev --scope=server",
    "dev": "lerna run dev",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "eslint-fix": "lerna run eslint-fix",
    "stylelint-fix": "lerna run stylelint-fix",
    "type-check": "lerna run type-check",
    "format": "lerna run format",
    "preview": "lerna run preview",
    "ssr-dev": "lerna run clean && lerna run ssr-build --scope=client && lerna run ssr-dev --scope=server",
    "ssr-prod": "lerna run clean && lerna run build --scope=client && lerna run ssr-build --scope=client && lerna run ssr-prod --scope=server"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=15"
  },
  "devDependencies": {
    "@evilmartians/lefthook": "^1.3.9",
    "lerna": "^5.4.3"
  }
}
